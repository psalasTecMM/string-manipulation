#ifndef MYSTRING_H
#define MYSTRING_H

int len (char*);

char* alphabet(char*);

char* concat(char*, char*);

char* spow(char*, int);

char* inverse(char*);

int ocurrences(char, char*);

int equal(char*, char*);

#endif